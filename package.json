{
  "name": "@here.build/foundations",
  "version": "0.0.0",
  "private": true,
  "description": "Foundation packages powering here.build — plexus, arrival, delta, and shared infrastructure.",
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  },
  "packageManager": "pnpm@10.3.0",
  "engines": {
    "node": ">=20"
  }
}
